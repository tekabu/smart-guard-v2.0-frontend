services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smart-guard-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - smart-guard-network

networks:
  smart-guard-network:
    driver: bridge
